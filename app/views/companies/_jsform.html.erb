<%= simple_form_for(@company) do |f| %>
	  <% if @company.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@company.errors.count, "error") %> prohibited this company from being saved:</h2>
	
	      <ul>
	      <% @company.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

  <div class="field">
    <%= f.input :name %>
  </div>
  <div class="field">
    <%= f.input :phone %>
  </div>
  <div class="field">
    <%= f.input :webpage %>
  </div>
  <div class="field">
    <%= f.input :contact %>
  </div>
  <div class="field">
    <%= f.input :address1 %>
  </div>
  <div class="field">

    <%= f.input :address2 %>
  </div>
 
  <div class="field">
    <%= f.select :country, get_countries, {},  id: 'country' %>
  </div>  

  <div class="field">
    <%= f.select :state, selected_state(@company), {}, id: 'state' %>
  </div>

  <div class="field">
    <%= f.select :city, selected_city(@company), {}, id: 'city' %>
  </div>

 
  <div class="actions btn btn-info">
    <%= f.button :submit %>
  </div>
<% end %>
